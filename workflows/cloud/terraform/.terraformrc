# ~/.terraformrc

# Plugin cache to speed up provider downloads
nplugin_cache_dir = "${HOME}/.terraform.d/plugin-cache"

# Terraform Cloud credentials (replace with your token)
credentials "app.terraform.io" {
  token = "YOUR_TERRAFORM_CLOUD_TOKEN"
}
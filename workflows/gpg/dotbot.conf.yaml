# GPG workflow Dotbot config

- shell:
    # Bootstrap GPG & Pinentry\    - [ bash, -lc, "$DOTFILES/workflows/gpg/install.sh" ]

- link:
    workflows/gpg/gpg.conf:          ~/.gnupg/gpg.conf
    workflows/gpg/gpg-agent.conf:    ~/.gnupg/gpg-agent.conf